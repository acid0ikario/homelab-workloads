apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: workloads
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

# CI opens a PR that updates this block with the new immutable SHA tag.
# Placeholder image until the first real CI build exists.
images:
  - name: ghcr.io/acid0ikario/garmindashboard
    newName: nginxdemos/hello
    newTag: plain-text
